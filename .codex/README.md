# Codex Local Notes

This directory is reserved for Codex-local repo configuration and notes.

- Primary repo instructions live in `../AGENTS.md`.
- Do not store secrets, generated artifacts, dependency directories, or local credentials here.
- Do not mirror `.claude/settings.local.json`; Claude permissions are not Codex policy.
- Add Codex-specific files here only when they are repo-portable and safe to commit.
